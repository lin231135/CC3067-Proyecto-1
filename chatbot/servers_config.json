{
  "servers": [
    {
      "name": "filesystem",
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "./workspace"],
      "description": "Official Anthropic Filesystem MCP server, sandboxed to ./workspace."
    },
    {
      "name": "git",
      "transport": "stdio",
      "command": "uvx",
      "args": ["mcp-server-git"],
      "description": "Official Anthropic Git MCP server."
    },
    {
      "name": "lims",
      "transport": "stdio",
      "enabled": false,
      "command": "python",
      "args": ["-m", "lims_mcp_server.server"],
      "description": "Custom local LIMS food-safety lab MCP server (this repository). Disabled by default now that the remote deployment below is live -- flip the 'enabled' flags to switch back to local."
    },
    {
      "name": "lims-remote",
      "transport": "http",
      "enabled": true,
      "base_url": "https://cc3067-lims-mcp.onrender.com",
      "description": "Same LIMS server as 'lims', deployed remotely over HTTP+SSE on Render (see docs/deployment.md for why Render instead of Cloud Run). Verified live: initialize, session handling, tools/list and tools/call all confirmed working end-to-end against this URL."
    }
  ]
}
